using UnityEngine;

public class menambahkannamanezar : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("nama saya adalah canezares keandre totiro");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
